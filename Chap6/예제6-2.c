/*#include <stdio.h>

int main(void)
{
	int a = 1;						// 변수를 선언하고 1로 초기화
	int i;							// 반복 횟수를 세기 위한 변수
									// 초기식 - 조건식 - 실행문 - 증감식 순으로 실행
	for (i = 0; i < 3; i++)			// i는 0으로 초기화된 후에 3보다 작은 동안 하나씩 증가하면서 실행
	{								 
		a = a * 2;					// 실행문
	}
	printf("a : %d\n", a);

	return 0;
}*/