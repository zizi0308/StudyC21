/*#include <stdio.h>

int func(int, int);

int main()
{
	int a, b;
	int result;								// result로 아래의 값을 main함수에 출력(return값을 전달받음)
	a = 10, b = 20;							// result에서 res의 값을 복사해서 사용(두 값은 다른 값임)

	result = func(a, b);
	printf("result : %d\n", result);
	return 0;
}

int func(int aa, int ab)
{
	int res;
	res = aa + ab;
	return res;								// 출력이 int로 되므로 return값이 있어야 함(호출)
}*/