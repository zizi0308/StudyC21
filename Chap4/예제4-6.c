/*#include <stdio.h>

int main()
{
	int a = 30;
	int res;

	res = (a > 10) && (a < 20);						// 0 (좌항, 우항이 모두 참이면 참)
	printf("(a > 10) && (a < 20) : %d\n", res);

	res = (a < 10) || (a > 20);						// 1 (좌항, 우항 중 하나라도 참이면 참)
	printf("(a < 10) || (a > 20) : %d\n", res);

	res = !(a >= 30);								// 0 (거짓이면 참으로, 참이면 거짓으로)
	printf("(!(a >= 30) : %d\n)", res);

	return 0;
}*/