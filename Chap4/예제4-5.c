/*#include <stdio.h>

int main()
{
	int a = 10, b = 20, c = 10;			// 피연산자
	int res;							// 결과값을 저장할 변수

	res = (a > b);						// 이항연산자
	printf("a > b : %d\n", res);		// 0 (거짓)

	res = (a >= b);						// 크거나 같다(같다(=)가 뒤에 위치)
	printf("a >= b : %d\n", res);		// 0

	res = (a < b);
	printf("a < b : %d\n", res);		// 1 (참) 사실 0이 아닌 모든 값은 참임

	res = (a <= b);
	printf("a <= b : %d\n", res);		// 1

	res = (a <= c);
	printf("a <= c : %d\n", res);		// 1

	res = (a == b);
	printf("a = b : %d\n", res);		// 0

	res = (a != c);
	printf("a != c : %d\n", res);		// 0

	return 0;
}*/