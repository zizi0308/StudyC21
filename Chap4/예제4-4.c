/*#include <stdio.h>

int main()
{
	int a = 5, b = 5;
	int pre, post;

	pre = (++a) * 3;		// 전위형 증감연산자  //증가하고 던짐 // 단항연산자
	post = (b++) * 3;		// 후위형 증감연산자  //던져주고증가

	printf("초깃값 a  = %d, b = %d\n", a, b);
	printf("전위형 : (a++) * 3 = %d, 후위형 : (++b) * 3 = %d\n", pre, post);

	int a = 10, b = 20;
	int c = 10, d = 20;
	++a;
	--b;
	c++;
	d--;
	printf("%d\t%d\n", a, b);
	printf("%d\t%d\n", c, d);

	return 0;
}*/