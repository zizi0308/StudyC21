/*#include <stdio.h>

int main()
{
	int a = 10, b = 20, res;

	a + b;									// 레지스터에 임시로 저장(저장X, 값 바로 날아감)
	printf("%d + %d = %d\n", a, b, a + b);	// 연산결과를 바로 출력해서 사용

	res = a + b;							// 결과값을 res라는 변수에 저장시킴
	printf("%d + %d = %d\n", a, b, res);	

	return 0;
}*/